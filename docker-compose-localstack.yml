---
version: "2.1"
services:
  localstack:
    image: localstack/localstack@sha256:87471106aed0299220dba34470d7099ae9229d436cade6eb5532223707ca1ce1
    network_mode: bridge
    environment:
      SERVICES: sns,s3,sqs
      DEBUG: 0
    ports:
      - 4566:4566
    volumes:
      - "${LOCALSTACK_VOLUME_DIR:-./volume}:/var/lib/localstack"
      - "/var/run/docker.sock:/var/run/docker.sock"
