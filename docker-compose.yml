---
services:
  gotenberg:
    image: gotenberg/gotenberg:8
    hostname: gotenberg
  queuelistener:
    build:
      context: .
      dockerfile: queuelistener.dockerfile
    depends_on:
      - gotenberg
    env_file:
      - ./.env
